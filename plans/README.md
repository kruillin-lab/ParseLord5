# Implementation Plans

Generated by the improve skill on 2026-08-25 against `e9b9d1789`. Execute in the order below unless dependencies say otherwise. Each executor: read the plan fully before starting, honor its STOP conditions, and update your row when done.

Repo: `C:\Users\kruil\Documents\Projects\ffxiv-tools\ParseLord5`
Branch constraint (AGENTS.md): at most one work branch besides `main`. Use a single branch `advisor/improve-f1-f5` for all five plans.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001  | Stop ASEX peek from bypassing the SGE DPS-lane blocklist | P1 | S | — | DONE (f395bd2e84, branch cleanup/remove-experiment-flags) |
| 002  | Repair the two structural lints HEAD already broke | P1 | S | 001 | DONE (21afbcb26) |
| 003  | Clear OverrideTarget on every ExecuteST/ExecuteAoE early return | P1 | S | — | DONE (2cf1e1e279) |
| 004  | Stamp the source checkout into devPlugins and refuse cross-tree overwrite | P1 | S | — | DONE (4788b8f7c) |
| 005  | Correct stale agent/handoff docs so agents build the right tree | P1 | S | — | DONE (AGENTS_FULL corrected; dated docs archived; wargames historical) |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- 002 requires 001 because 001 changes `!asRedirected && !CanUseAutorotDpsAction(outAct)` to always call `CanUseAutorotDpsAction`. 002 rewrites the two red regexes against that post-fix shape. Landing 002 first would encode the bypass as intended.
- 003 touches the same `ExecuteST` / `ExecuteAoE` methods as 001. Do 001 first if both run in one branch so the blocklist change is not mixed into the try/finally diff. 003 does not depend on 002.
- 004 and 005 are independent of 001–003 and of each other.

## Findings considered and not planned this run

- F6 Healer Manual targeting footgun (`ManualTarget()` has no `GetLowestCurrent()` fallback). Real, deferred: CLAUDE.md already tells operators to check `HealerRotationMode` first. Changing default targeting is a behavior change that needs its own plan.
- F7 SCH/AST DPS combos still return raidwides during the DPS probe. Real, deferred: raidwide helpers already require `GroupDamageIncoming()`, so this is not the pull-start dump. Needs a dedicated lint plus WHM-style gates. Do not apply those gates to tanks/DPS.
- F8 Extract ASEX peek/OverrideTarget into a pure function and unit-test it. Deferred until 002 is green; needs a Dalamud-free seam.
- F9 GitHub Actions for `dotnet test`. Deferred; 002 restores the local gate first.
- D1 Dedicated utility/mitigation autorot lane. Direction spike, not a bug fix. Roadmap forbids an architecture rewrite up front.
- D2 Finish SmartMit shared engine. Direction. Four job copies (541–728 lines) still exist; characterization tests for the calculator already exist.
- D3 Delete or merge `C:\Users\kruil\orca\ParseLord5`. Human decision. 004 makes the collision fail-closed without deleting the other tree.
- Namespace rename away from `WrathCombo.*`, importing RSR, applying healer DPS-lane gates to 19 tank/DPS jobs, CI of the Dalamud plugin build, coverage-percentage target: not worth doing.

## Verification baseline at plan time

`dotnet test WrathCombo.Tests/WrathCombo.Tests.csproj -c Release` on `e9b9d1789`: Failed 2, Passed 54.

Failing facts:

- `RotationStructureTests.AutorotationProbeContext_IsOptIn` (line 202)
- `RotationStructureTests.AutorotationDpsLane_BlocksSgeDefensiveActions` (line 123)
